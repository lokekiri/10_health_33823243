<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Workouts - Fittessness</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body>
    <header>
        <nav>
            <a href="/" class="logo">Fittessness</a>
            <ul>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/add-workout">Add Workout</a></li>
                <li><a href="/list-workouts">My Workouts</a></li>
                <li><a href="/search-workouts">Search</a></li>
                <li><a href="/profile">Profile</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <div class="container">
            <h1>Search Workouts</h1>
            <p>Find specific workouts by exercise name</p>

            <div class="search-container">
                <form action="/search-workouts" method="GET" class="search-bar">
                    <input type="text" name="keyword" placeholder="Search by exercise name..." value="<%= keyword || '' %>">
                    <button type="submit" class="btn btn-primary">Search</button>
                </form>
            </div>

            <% if (searched) { %>
                <% if (results && results.length > 0) { %>
                    <h2>Search Results (<%= results.length %>)</h2>
                    <table>
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Exercise</th>
                                <th>Duration (mins)</th>
                                <th>Calories</th>
                                <th>Notes</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% results.forEach(function(workout) { %>
                                <tr>
                                    <td><%= new Date(workout.date).toLocaleDateString() %></td>
                                    <td><%= workout.exercise %></td>
                                    <td><%= workout.duration %></td>
                                    <td><%= workout.calories %></td>
                                    <td><%= workout.notes || '-' %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="message">
                        <p>No workouts found matching "<%= keyword %>". Try a different search term.</p>
                    </div>
                <% } %>
            <% } else { %>
                <p>Enter an exercise name above to search your workout history.</p>
            <% } %>

            <div style="margin-top: 20px;">
                <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
            </div>
        </div>
    </main>

    <footer>
        <p>&copy; 2025 Fittessness. Built for health and fitness enthusiasts.</p>
    </footer>
</body>
</html>
